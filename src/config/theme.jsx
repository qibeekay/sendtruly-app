// theme.js
import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    // ... other color schemes
    customRed: '#fc1e65', // Create a new color scheme 'customRed'
  },
});

export default theme;
